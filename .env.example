# Server-side secrets only. Never paste credentials into chat or client code.
OPENAI_API_KEY=
LLM_MODEL=gpt-4.1-mini-2025-04-14
BRAIN_PROVIDER=openai
RIME_API_KEY=
PORT=8765
TOOL_DELAY_S=3.0
# Explicit development mode only: 1 emits a hum instead of Rime speech.
RIME_DEV_STUB=0
