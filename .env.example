# Copy to .env and fill in. NEVER commit .env.
RIME_API_KEY=your_rime_api_key_here

# Optional. If unset, the agent uses a scripted stub brain so the
# heard-ledger demo runs with zero LLM dependency.
ANTHROPIC_API_KEY=
